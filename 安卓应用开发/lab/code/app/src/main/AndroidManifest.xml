<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sliding">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sliding"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".student.QuestionActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".WelcomeActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".teacher.QuestionActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".teacher.ForestActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".teacher.ChooseActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".student.ForestActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <activity android:name=".student.QuestionOrForestActivity"
            android:theme="@style/AppTheme.Translucent"
            ></activity>

        <service android:name=".student.ForestService"></service>
    </application>

</manifest>